#!/usr/bin/env bash

KEYWORD="nocheckin"

FOUND=$(git diff --cached -- ':!*pre-commit' | grep $KEYWORD | wc -l)

if [[ $FOUND -gt 0 ]]; then
    echo "Keyword: $KEYWORD detected. Cannot commit."
    exit 1
fi
